in:
  type: file
  path_prefix: example/input
  parser:
    type: jsonpath
    root: "$.items"
    columns:
      - { name: "sticker_price", type: string }
      - { name: "review_ranking_in_category", type: string }
out:
  type: stdout
